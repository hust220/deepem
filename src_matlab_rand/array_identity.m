function a = array_identity( array )
%ARRAY_IDENTITY 此处显示有关此函数的摘要
%   此处显示详细说明

fprintf('trace: %f, sum: %f, max: %f, min: %f', trace(array), sum(array), max(array), min(array));
a = 1;

end

